<section id="contact" class="contact-me-container">
  <h2 class="contact-title">Contact Me</h2>
  <p class="contact-description">
    Let’s connect! Reach out with any inquiries about my music services.
  </p>

  <div class="contact-card">
    <h3 class="contact-card-title">Ready to get in contact?</h3>
    <form
      action="https://formsubmit.co/smithjacob2021@gmail.com"
      method="POST"
      class="contact-form"
    >
      <input
        type="text"
        name="name"
        placeholder="Your Name"
        required
        class="form-input"
      />
      <input
        type="email"
        name="email"
        placeholder="Your Email"
        required
        class="form-input"
      />
      <textarea
        name="message"
        placeholder="What are you looking for?"
        required
        class="form-textarea"></textarea>
      <button type="submit" class="form-button">Send Message</button>
    </form>
  </div>
</section>

<style>
  .contact-me-container {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    padding: 4rem 1rem;
    max-width: 900px;
    margin: 0 auto;
  }

  .contact-title {
    font-size: 2.5rem;
    font-weight: 900;
    color: white;
    margin-bottom: 1rem;
  }

  .contact-description {
    font-size: 1.125rem;
    color: #cccccc;
    margin-bottom: 2rem;
  }

  .contact-links {
    display: flex;
    gap: 2rem;
    flex-wrap: wrap;
    justify-content: center;
  }

  .contact-link {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.75rem 1.5rem;
    border-radius: 9999px;
    font-weight: bold;
    text-decoration: none;
    font-size: 1.125rem;
    transition:
      background-color 0.3s,
      transform 0.3s;
  }

  .contact-link:hover {
    transform: scale(1.05);
  }

  .contact-card {
    background-color: #1f2937; /* dark gray card */
    padding: 2rem;
    border-radius: 1.5rem;
    margin-top: 3rem;
    width: 100%;
    max-width: 500px;
    box-shadow: 0 8px 20px rgba(0, 0, 0, 0.6);
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .contact-card-title {
    font-size: 1.75rem;
    font-weight: 700;
    margin-bottom: 1.5rem;
    color: white;
    text-align: center;
  }

  /* Update form and inputs slightly */

  .contact-form {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    width: 100%;
  }

  .form-input,
  .form-textarea,
  .form-button {
    width: 100%;
    box-sizing: border-box; /* ADD THIS LINE */
  }

  .form-input,
  .form-textarea {
    padding: 0.75rem 1rem;
    border-radius: 0.75rem;
    border: none;
    font-size: 1rem;
  }

  .form-textarea {
    min-height: 150px;
    resize: vertical;
  }

  .form-button {
    background-color: #1db954;
    color: white;
    padding: 0.75rem 1.5rem;
    font-weight: bold;
    font-size: 1.125rem;
    border: none;
    border-radius: 9999px;
    cursor: pointer;
    transition:
      background-color 0.3s,
      transform 0.3s;
  }

  .form-button:hover {
    background-color: #159b43;
    transform: scale(1.05);
  }
  @media (max-width: 640px) {
    .contact-me-container {
      padding: 3rem 1.5rem; /* increase side padding for breathing room */
    }

    .contact-title {
      font-size: 2rem;
    }

    .contact-description {
      font-size: 1rem;
    }

    .contact-links {
      align-items: center; /* center items horizontally */
      gap: 1.5rem;
      width: 100%;
    }

    .contact-link {
      justify-content: center;
      width: 100%;
      max-width: 300px;
      margin: 0 auto;
    }

    .contact-card {
      width: 100%;
      max-width: 500px; /* Add a max-width for card itself */
      padding: 1.5rem;
      padding-right: 1rem;
      padding-left: 1rem;
      border-radius: 1.25rem;
      margin-top: 2rem;
      margin-left: 10px;
      margin-right: 10px;
    }

    .contact-card-title {
      font-size: 1.5rem;
      margin-bottom: 1rem;
    }

    .contact-form {
      gap: 1.25rem;
      width: 100%;
    }

    .form-input,
    .form-textarea,
    .form-button {
      font-size: 1rem;
      padding: 0.75rem;
    }

    .form-button {
      font-size: 1rem;
    }
  }
</style>
